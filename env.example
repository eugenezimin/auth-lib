# PostgreSQL connection – used by setup_db and the application itself
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=auth          # optional – defaults to "auth" if omitted
